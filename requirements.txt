# HDEG 依赖清单
# 安装: pip install -r requirements.txt
# 主流程仅需「必装」项；其余为增强功能，缺失时程序会自动回退，不影响主流程。

# ── 必装（主流程）──

# LLM API（翻译 / 台本识别 / 分割）
openai>=1.0.0

# 台本 PDF 文本提取
PyMuPDF>=1.23.0

# ── 增强（缺失自动回退）──

# 日文分词（术语分析 / 一致性检查；不可用时回退简单规则）
fugashi>=1.2.0
unidic-lite>=1.0.8

# 日文汉字 → 假名读音（台本对齐；Windows 上需预编译 wheel，缺失时自动回退纯假名/汉字比较）
pyopenjtalk>=0.3.0

# ── 可选（体积大，按需启用）──

# OCR 台本识别（PaddleOCR，仅当使用扫描/图片台本提取时启用）
# paddleocr
# paddlex
